# Namma Metro data

Add JSON or CSV here for:

- Purple Line stations (ordered)
- Green Line stations (ordered)
- Interchange nodes (e.g. Majestic)
- Fare slabs / distance matrix

The route engine will load this via `src/lib/metro/loader.ts` (Phase 2).
